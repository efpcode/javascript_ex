/* 1. Grab the input value */

/* The challenge is to create sum function that returns 
 * the sum of two integers or floats.
 * */

function sum(a, b){
	return a + b
};

document.body.innerHTML = sum(3,3);

